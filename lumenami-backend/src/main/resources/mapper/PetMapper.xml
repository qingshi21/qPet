<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lumenami.backend.mapper.PetMapper">

    <resultMap id="PetResult" type="com.lumenami.backend.model.Pet">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="name" column="name"/>
        <result property="roleName" column="role_name"/>
        <result property="systemPrompt" column="system_prompt"/>
        <result property="isActive" column="is_active"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.lumenami.backend.model.Pet" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO pets (user_id, name, role_name, system_prompt, is_active)
        VALUES (#{userId}, #{name}, #{roleName}, #{systemPrompt}, #{isActive})
    </insert>

    <select id="findByUserId" resultMap="PetResult">
        SELECT * FROM pets WHERE user_id = #{userId}
    </select>

    <select id="findById" resultMap="PetResult">
        SELECT * FROM pets WHERE id = #{petId}
    </select>

    <update id="deactivateAllByUserId">
        UPDATE pets SET is_active = 0 WHERE user_id = #{userId}
    </update>

    <update id="activate">
        UPDATE pets SET is_active = 1 WHERE id = #{petId}
    </update>

    <delete id="deleteById">
        DELETE FROM pets WHERE id = #{petId}
    </delete>

    <select id="countByNameAndUserId" resultType="int">
        SELECT COUNT(*) FROM pets WHERE user_id = #{userId} AND name = #{name}
    </select>

</mapper>